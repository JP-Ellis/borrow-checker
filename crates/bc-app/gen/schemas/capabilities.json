{
  "default": {
    "identifier": "default",
    "description": "Default capability set for the borrow-checker app",
    "local": true,
    "windows": ["main"],
    "permissions": ["core:default"]
  }
}
