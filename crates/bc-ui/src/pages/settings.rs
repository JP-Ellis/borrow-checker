//! Settings — FY start, fortnightly anchor, display currency, theme, data.

use leptos::prelude::*;

/// Settings page stub.
#[component]
pub fn Settings() -> impl IntoView {
    view! { <div class="page page-settings">"settings — coming in phase 2"</div> }
}
