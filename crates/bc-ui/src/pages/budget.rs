//! Budget — allocation grid + quick-allocate terminal.

use leptos::prelude::*;

/// Budget page stub.
#[component]
pub fn Budget() -> impl IntoView {
    view! { <div class="page page-budget">"budget — coming in phase 2"</div> }
}
