//! Accounts — account tree sidebar + transaction register.

use leptos::prelude::*;

/// Accounts page stub.
#[component]
pub fn Accounts() -> impl IntoView {
    view! { <div class="page page-accounts">"accounts — coming in phase 2"</div> }
}
