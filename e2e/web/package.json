{
  "name": "bc-e2e-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
