[package]
name    = "bc-plugin-ledger"
version = "0.1.0"
edition = "2024"

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bc-sdk       = { path = "../../crates/bc-sdk" }
rust_decimal = "1"

[dev-dependencies]
pretty_assertions   = "1"
rust_decimal_macros = "1"
